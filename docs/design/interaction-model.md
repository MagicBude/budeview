# Interaction Model

**Image first, controls on demand.**

默认状态主要区域只有图片；鼠标移动出现少量 Viewer 控件；右键出现完整上下文菜单；`I` 打开信息 Overlay；Filmstrip 临时调出但不升级为 Library Browser。

Windows Explorer 仍然是主要文件浏览器。BudeView 可以知道当前目录里的图片，但不引入文件夹树、数据库、标签、Catalog、Album。
