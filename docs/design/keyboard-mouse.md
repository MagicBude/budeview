# Keyboard & Mouse

默认约定：

- `←` 上一张
- `→` 下一张
- `Home` 第一张
- `End` 最后一张
- `F` Fit Window
- `1` 100%
- `F11` Fullscreen
- `I` Image Info
- Wheel 缩放
- 缩放锚点为鼠标位置
- Left Drag：放大后 Pan
- Right Click：上下文菜单

核心动作未来必须可重绑定，动作层不能硬编码到控件层。
